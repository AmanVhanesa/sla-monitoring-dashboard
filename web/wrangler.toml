# The dashboard is static files only - no server code. Cloudflare serves the
# built bundle directly and falls back to index.html so client-side routing and
# deep links work.
name = "dashboard"
compatibility_date = "2025-09-01"

[observability]
enabled = true

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
